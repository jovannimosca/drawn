---
phase: quick-260418-jx9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .husky/commit-msg
  - .commitlintrc.json
  - .github/workflows/quality.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Commits must follow conventional commit format"
    - "Invalid commit messages are blocked at pre-commit"
    - "CI enforces conventional commit format on PRs"
  artifacts:
    - path: .husky/commit-msg
      provides: "Pre-commit hook that validates commit message format"
    - path: .commitlintrc.json
      provides: "Conventional commit rules configuration"
      contains: "rules for type, scope, subject"
    - path: package.json
      provides: "Husky and commitlint dependencies"
      contains: "husky, @commitlint/cli, @commitlint/config-conventional"
    - path: .github/workflows/quality.yml
      provides: "CI commitlint check"
      contains: "Commitlint Check"
  key_links:
    - from: package.json
      to: .husky/commit-msg
      via: "husky install in postinstall"
    - from: .husky/commit-msg
      to: .commitlintrc.json
      via: "commitlint --edit $1"
---

<objective>
Implement conventional commits enforcement with pre-commit hooks and CI quality gate.

Purpose: Ensure all commits follow the conventional commits specification (`<type>(<scope>): <subject>`) for semantic-release to properly generate release notes and determine version bumps.

Output: Husky pre-commit hook + commitlint + CI check
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.github/workflows/quality.yml
@.github/workflows/ci.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Husky and commitlint dependencies</name>
  <files>package.json</files>
  <action>
Update package.json to add devDependencies for:
- husky (^9.x) - Git hooks manager
- @commitlint/cli (^19.x) - Lint commit messages
- @commitlint/config-conventional (^19.x) - Conventional commits rules

Also add scripts:
- "prepare": "husky install" - auto-installs husky hooks on npm install
</action>
  <verify>npm install runs without errors and node_modules/.bin contains husky and commitlint</verify>
  <done>package.json updated with husky, commitlint, and prepare script</done>
</task>

<task type="auto">
  <name>Task 2: Create commitlint configuration</name>
  <files>.commitlintrc.json</files>
  <action>
Create .commitlintrc.json with the following rules:
- type-enum: [feat, fix, docs, style, refactor, test, chore, revert] - required
- type-case: lowerCase
- type-empty: disallow empty type
- subject-empty: disallow empty subject
- subject-full-stop: disallow trailing period
- header-max-length: 100 characters

Use @commitlint/config-conventional as extends.
</action>
  <verify>File exists with valid JSON and includes conventional rules</verify>
  <done>.commitlintrc.json created with conventional commit rules</done>
</task>

<task type="auto">
  <name>Task 4: Add commitlint CI check to quality workflow</name>
  <files>.github/workflows/quality.yml</files>
  <action>
Add a "Commitlint Check" step after the checkout step in .github/workflows/quality.yml:

```yaml
      - name: Commitlint Check
        uses: wagoid/commitlint-github-action@v5
        with:
          fromHead: origin/main
          toHead: HEAD
```

This runs commitlint on PR commits to enforce conventional commit format in CI.
  </action>
  <verify>Commitlint check appears in quality.yml workflow</verify>
  <done>Commitlint CI check added to quality workflow</done>
</task>

<task type="auto">
  <name>Task 3: Configure Husky pre-commit hook</name>
  <files>.husky/commit-msg</files>
  <action">
Create .husky/commit-msg file that runs commitlint on commit message:
```
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npx --no -- commitlint --edit ${1}
```

Make the file executable: chmod +x .husky/commit-msg

Then run: npx husky install to initialize husky in the project.
</action>
  <verify">.husky/commit-msg exists and is executable; running 'git commit -m "chore: test"' should pass, 'git commit -m "bad message"' should fail</verify>
  <done>Husky hook installed and validates commit messages on commit</done>
</task>

</tasks>

<verification>
Manual verification steps:
1. Run `git commit -m "feat: add new feature"` — should succeed
2. Run `git commit -m "bad message"` — should fail with commitlint error
3. Run `npm run prepare` — should show "husky - Git hooks installed"
</verification>

<success_criteria>
- Conventional commits format enforced on local commits
- Invalid commit messages blocked before commit completes
- Semantic-release can analyze conventional commits for release versioning
</success_criteria>

<output>
After completion, create .planning/quick/260418-jx9-implement-conventional-commits-enforceme/260418-jx9-SUMMARY.md
</output>