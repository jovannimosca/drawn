---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Release workflow auto-bumps version based on conventional commits"
    - "Release notes are generated automatically from commit messages"
    - "GitHub release is created with proper version tag"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Semantic-release powered release workflow"
      min_lines: 100
  key_links: []
---

<objective>
Migrate release workflow from client-side script to semantic-release for auto-bumps and automatic release notes
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan-quick.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/release.yml
@app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate release workflow to semantic-release</name>
  <files>.github/workflows/release.yml</files>
  <action>Replace the current release workflow with semantic-release. Key changes:

1. Add semantic-release@latest action as the first step after checkout
2. Remove "Bump version" step (replaced by semantic-release)
3. Keep "Build Release APK" step but use env vars from semantic-release outputs
4. Replace "Generate release notes" step - semantic-release generates release notes automatically
5. Update "Create GitHub Release" step to use semantic-release's outputs

Configuration:
- branches: main
- plugins: @semantic-release/commit-analyzer, @semantic-release/release-notes-generator, @semantic-release/github
- outputVariables: true (to pass VERSION_NAME to subsequent steps)

The semantic-release action outputs:
- VERSION_NAME (e.g., "1.2.3")
- RELEASE_TAG (e.g., "v1.2.3")

Remove these legacy elements:
- All manual version parsing/bumping in "Bump version" step
- Manual release note generation in "Generate release notes" step
- workflow_dispatch version overrides (semantic-release handles this)

Keep:
- Path filters for CI optimization
- workflow_dispatch with skip_release option for build-only scenarios
- Keystore decoding and build steps</action>
  <verify>grep -q "semantic-release@latest" .github/workflows/release.yml</verify>
  <done>Release workflow uses semantic-release, auto-bumps version based on conventional commits, generates release notes automatically, creates GitHub release with version tag</done>
</task>

</tasks>

<verification>
- Release workflow exists at .github/workflows/release.yml
- semantic-release action is configured
- Manual version bumping removed
- Manual release note generation removed
</verification>

<success_criteria>
- Workflow uses semantic-release for version management
- Release notes are auto-generated from conventional commits
- GitHub release is created with proper tag
</success_criteria>

<output>
After completion, create `.planning/quick/260418-h7x-semantic-release-migration/260418-h7x-SUMMARY.md`
</output>