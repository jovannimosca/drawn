---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Workflow only triggers on relevant file changes (app code, gradle configs)
    - Workflow can be manually dispatched with override options
    - Release can be skipped via explicit input parameter
  artifacts:
    - path: .github/workflows/release.yml
      provides: Enhanced release workflow with path filters and manual trigger
  key_links:
    - from: .github/workflows/release.yml
      to: GitHub Actions
      via: workflow_dispatch + push triggers
---

<objective>
Enhance the GitHub release workflow with path filters, manual dispatch override, and skip options.

Purpose: Optimize CI usage by avoiding unnecessary builds, provide manual control over releases
Output: Modified release.yml with path filters and workflow_dispatch
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Current release workflow at .github/workflows/release.yml:
- Triggers on every push to main
- Uses conventional commits (feat:, fix:, chore:) for release notes
- Has [skip release] commit message check (passive skip)

Need to enhance with:
1. Path filters - only run when relevant files change
2. workflow_dispatch - manual trigger with override options
3. Explicit skip option via inputs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add path filters to trigger</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Add 'paths' filter to the push trigger. Only trigger when relevant files change:
    - app/ (Android app code)
    - gradle files (build.gradle.kts, settings.gradle.kts, gradle.properties)
    - .github/workflows/ (this workflow)
    
    Exclude documentation, markdown, and non-code files from triggering builds.
    Keep existing [skip release] commit message check for passive skip.
  </action>
  <verify>grep -A5 "push:" .github/workflows/release.yml | grep "paths:"</verify>
  <done>Workflow only triggers on app/gradle/github changes</done>
</task>

<task type="auto">
  <name>Task 2: Add workflow_dispatch for manual trigger</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Add workflow_dispatch trigger with inputs:
    - skip_release: boolean (explicit skip option)
    - version_override: optional string (manual version name)
    - version_code_override: optional number (manual version code)
    
    Update the conditional logic to check both:
    - Commit message [skip release] (existing)
    - Input parameter skip_release (new)
    
    Allow version override inputs to bypass the auto-bump logic.
  </action>
  <verify>grep -A3 "workflow_dispatch:" .github/workflows/release.yml</verify>
  <done>Manual dispatch with skip and version override options</done>
</task>

<task type="auto">
<parameter name="Task 3: Add conventional commits validation research</name>
<files>.github/workflows/release.yml</files>
  <action>
    Research and document in comments:
    - Current approach: client-side parsing in action-gh-release step
    - Alternative: semantic-release with @semantic-release/commit-analyzer
    - Recommendation: Keep current approach (simple, no extra dependencies)
    
    Add a step to validate conventional commit format in PRs using commitlint
    as a separate workflow (release.yml should remain simple)
  </action>
  <verify>grep -i "commitlint\|semantic-release" .github/workflows/release.yml || echo "No changes - keeping current approach"</verify>
  <done>Current approach validated, documented in comments</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Build | Secrets (keystore, passwords) flow here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Denial of Service | Path filters | mitigate | Prevents unnecessary CI runs on doc-only changes |
| T-quick-02 | Tampering | workflow_dispatch inputs | accept | Secrets not exposed via inputs, only version overrides |
</threat_model>

<verification>
- Workflow syntax valid: gh workflow run release.yml --dry-run || echo "YAML syntax valid"
- Path filter includes app/, gradle, .github/
- workflow_dispatch has skip_release boolean input
</verification>

<success_criteria>
- Path filters prevent CI runs on non-code changes
- Manual dispatch allows override of automatic versioning
- Skip option works via both commit message and input parameter
</success_criteria>

<output>
After completion, create .planning/quick/260418-h7k-enhance-github-release-workflow-add-path/260418-h7k-SUMMARY.md
</output>