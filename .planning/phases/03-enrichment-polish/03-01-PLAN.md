---
phase: 03-enrichment-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt
  - app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt
  - app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt
autonomous: true
requirements: [THEME-01, THEME-02]
user_setup: []

must_haves:
  truths:
    - "App displays with dark purple/gold color scheme on all screens"
    - "Typography uses defined scale with 400/500 weights only"
    - "Cards have elevation/shadow effects"
    - "ReadingListScreen has subtle starry gradient background"
    - "Spacing follows formalized 4.dp multiple scale"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt"
      provides: "Custom typography scale"
      contains: "Typography("
      min_lines: 20
    - path: "app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt"
      provides: "DarkColorScheme with all M3 color slots"
      contains: "darkColorScheme("
      min_lines: 25
  key_links:
    - from: "app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt"
      to: "app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt"
      via: "color references"
      pattern: "DarkPrimary|DarkSecondary|DarkBackground"
    - from: "app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt"
      to: "app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt"
      via: "typography assignment"
      pattern: "typography = DrawnTypography"
---

<objective>
Define and apply the dark mystical theme foundation — typography scale, color scheme polish, and card elevation — that all Phase 3 screens will build on.

Purpose: Establishes the visual foundation (THEME-01, THEME-02) that Plans 02-05 consume. Without this, subsequent UI work would lack consistent styling.
Output: Refined Color.kt, Theme.kt, Type.kt with production-ready dark theme.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-enrichment-polish/03-CONTEXT.md
@.planning/phases/03-enrichment-polish/03-UI-SPEC.md

@.planning/phases/02-core-recording-loop/02-01-SUMMARY.md
@.planning/phases/02-core-recording-loop/02-02-SUMMARY.md
@.planning/phases/02-core-recording-loop/02-03-SUMMARY.md
@.planning/phases/02-core-recording-loop/02-04-SUMMARY.md

@app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt
@app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt
@app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt (current):
```kotlin
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceVariant = Color(0xFF2C2C2C)
val DarkPrimary = Color(0xFF9C27B0)
val DarkOnPrimary = Color(0xFFFFFFFF)
val DarkSecondary = Color(0xFFFFD700)
val DarkOnSecondary = Color(0xFF000000)
val DarkTertiary = Color(0xFF7B1FA2)
val DarkOnTertiary = Color(0xFFFFFFFF)
val DarkError = Color(0xFFCF6679)
val DarkOnError = Color(0xFF000000)
```

From app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt (current):
```kotlin
private val DarkColorScheme = darkColorScheme(
    primary = DarkPrimary,
    onPrimary = DarkOnPrimary,
    secondary = DarkSecondary,
    onSecondary = DarkOnSecondary,
    tertiary = DarkTertiary,
    onTertiary = DarkOnTertiary,
    error = DarkError,
    onError = DarkOnError,
    background = DarkBackground,
    onBackground = Color(0xFFE0E0E0),
    surface = DarkSurface,
    onSurface = Color(0xFFE0E0E0),
    surfaceVariant = DarkSurfaceVariant,
    onSurfaceVariant = Color(0xFFB0B0B0)
)

@Composable
fun DrawnTheme(content: @Composable () -> Unit) {
    MaterialTheme(
        colorScheme = DarkColorScheme,
        typography = DrawnTypography,
        content = content
    )
}
```

From app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt (current):
```kotlin
val DrawnTypography = Typography()  // Empty — uses M3 defaults
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define typography scale in Type.kt</name>
  <files>app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt (current empty state)
    - .planning/phases/03-enrichment-polish/03-UI-SPEC.md (Typography section, lines 52-68)
  </read_first>
  <acceptance_criteria>
    - Type.kt contains `val DrawnTypography = Typography(` with at least 5 custom TextStyle definitions
    - `headlineSmall` defined: ~24.sp, weight 400
    - `bodyLarge` defined: ~16.sp, weight 400
    - `bodyMedium` defined: ~14.sp, weight 400
    - `labelLarge` defined: ~14.sp, weight 500
    - `labelSmall` defined: ~11.sp, weight 400
    - Only weights 400 and 500 used (no FontWeight.Bold or FontWeight.SemiBold)
    - File compiles: `./gradlew :app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <action>
Replace the empty `val DrawnTypography = Typography()` in Type.kt with a full Typography definition. Per 03-UI-SPEC.md Typography table:

```kotlin
val DrawnTypography = Typography(
    displayLarge = TextStyle(
        fontSize = 57.sp,
        fontWeight = FontWeight(400),
        lineHeight = 64.sp,
        letterSpacing = (-0.25).sp
    ),
    headlineSmall = TextStyle(
        fontSize = 24.sp,
        fontWeight = FontWeight(400),
        lineHeight = 32.sp
    ),
    titleMedium = TextStyle(
        fontSize = 16.sp,
        fontWeight = FontWeight(400),
        lineHeight = 24.sp
    ),
    bodyLarge = TextStyle(
        fontSize = 16.sp,
        fontWeight = FontWeight(400),
        lineHeight = 24.sp
    ),
    bodyMedium = TextStyle(
        fontSize = 14.sp,
        fontWeight = FontWeight(400),
        lineHeight = 20.sp
    ),
    labelLarge = TextStyle(
        fontSize = 14.sp,
        fontWeight = FontWeight(500),
        lineHeight = 20.sp
    ),
    labelSmall = TextStyle(
        fontSize = 11.sp,
        fontWeight = FontWeight(400),
        lineHeight = 16.sp
    )
)
```

Add required imports: `androidx.compose.ui.text.TextStyle`, `androidx.compose.ui.text.font.FontWeight`, `androidx.compose.ui.unit.sp`.

Per D-56 (custom typography scale) and UI-SPEC typography section: exactly 2 weights — 400 for body/heading, 500 for labels only. No bold (700) or semibold (600).
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin --quiet 2>&1 | grep -c "error" || true</automated>
  </verify>
  <done>Type.kt exports DrawnTypography with 7 custom TextStyle definitions, only weights 400 and 500 used, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Polish color scheme and add missing M3 color slots</name>
  <files>app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt, app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt (current colors)
    - app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt (current color scheme)
    - .planning/phases/03-enrichment-polish/03-UI-SPEC.md (Color section, lines 70-103)
  </read_first>
  <acceptance_criteria>
    - Color.kt contains at least 15 color values (existing 11 + new ones)
    - Color.kt adds: DarkOutline, DarkOnSurface, DarkOnSurfaceVariant, DarkErrorContainer, DarkOnErrorContainer, DarkPrimaryContainer, DarkOnPrimaryContainer
    - Theme.kt DarkColorScheme includes ALL M3 color slots: primary, onPrimary, primaryContainer, onPrimaryContainer, secondary, onSecondary, tertiary, onTertiary, error, onError, errorContainer, onErrorContainer, background, onBackground, surface, onSurface, surfaceVariant, onSurfaceVariant, inverseSurface, inverseOnSurface, inversePrimary, surfaceTint, outline, outlineVariant, scrim
    - DarkColorScheme uses the new color values from Color.kt (not hardcoded hex values)
    - File compiles: `./gradlew :app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <action>
In Color.kt, add these new color values after the existing ones:

```kotlin
// Additional M3 color slots
val DarkOutline = Color(0xFF555555)
val DarkOnSurface = Color(0xFFE0E0E0)
val DarkOnSurfaceVariant = Color(0xFFB0B0B0)
val DarkErrorContainer = Color(0xFF93000A)
val DarkOnErrorContainer = Color(0xFFFFDAD6)
val DarkPrimaryContainer = Color(0xFF7B1FA2)
val DarkOnPrimaryContainer = Color(0xFFF3E5F5)
val DarkInverseSurface = Color(0xFFE0E0E0)
val DarkInverseOnSurface = Color(0xFF1E1E1E)
val DarkInversePrimary = Color(0xFF7B1FA2)
val DarkSurfaceTint = Color(0xFF9C27B0)
val DarkOutlineVariant = Color(0xFF3C3C3C)
val DarkScrim = Color(0x99000000)
```

In Theme.kt, expand DarkColorScheme to include ALL M3 color slots:

```kotlin
private val DarkColorScheme = darkColorScheme(
    primary = DarkPrimary,
    onPrimary = DarkOnPrimary,
    primaryContainer = DarkPrimaryContainer,
    onPrimaryContainer = DarkOnPrimaryContainer,
    secondary = DarkSecondary,
    onSecondary = DarkOnSecondary,
    tertiary = DarkTertiary,
    onTertiary = DarkOnTertiary,
    error = DarkError,
    onError = DarkOnError,
    errorContainer = DarkErrorContainer,
    onErrorContainer = DarkOnErrorContainer,
    background = DarkBackground,
    onBackground = DarkOnSurface,
    surface = DarkSurface,
    onSurface = DarkOnSurface,
    surfaceVariant = DarkSurfaceVariant,
    onSurfaceVariant = DarkOnSurfaceVariant,
    inverseSurface = DarkInverseSurface,
    inverseOnSurface = DarkInverseOnSurface,
    inversePrimary = DarkInversePrimary,
    surfaceTint = DarkSurfaceTint,
    outline = DarkOutline,
    outlineVariant = DarkOutlineVariant,
    scrim = DarkScrim
)
```

Replace hardcoded `Color(0xFFE0E0E0)` and `Color(0xFFB0B0B0)` in Theme.kt with the named constants from Color.kt. Per D-60: colors already defined, this is about application completeness, not new colors. Per UI-SPEC Color section: 60/30/10 ratio maintained.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin --quiet 2>&1 | grep -c "error" || true</automated>
  </verify>
  <done>Color.kt has 24+ color values, Theme.kt DarkColorScheme includes all 25 M3 color slots using named constants, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 3: Add card elevation defaults and verify theme compilation</name>
  <files>app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt (after Task 2 changes)
    - .planning/phases/03-enrichment-polish/03-UI-SPEC.md (Theme Polish section, lines 206-217)
  </read_first>
  <acceptance_criteria>
    - Theme.kt contains a `val DrawnCardElevation` or similar CardElevation definition
    - Elevation uses M3 CardDefaults.cardElevation() with defaultElevation = 1.dp and draggedElevation = 3.dp
    - Full build succeeds: `./gradlew :app:assembleDebug` completes without errors
  </acceptance_criteria>
  <action>
In Theme.kt, after the DarkColorScheme definition, add a card elevation constant that all card components will reference:

```kotlin
import androidx.compose.material3.CardDefaults
import androidx.compose.ui.unit.dp

val DrawnCardElevation = CardDefaults.cardElevation(
    defaultElevation = 1.dp,
    draggedElevation = 3.dp
)
```

This provides a single source of truth for card shadows across the app. Per UI-SPEC Theme Polish section: "All cards use M3 CardDefaults.cardElevation() with default elevation (1.dp resting, 3.dp dragged). No custom shadow values." Per D-57: card shadows and elevation for depth.

Run a full build to verify the theme compiles and all existing screens still render correctly with the updated color scheme and typography.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>DrawnCardElevation defined in Theme.kt with 1.dp default / 3.dp dragged, full debug build succeeds with zero errors</done>
</task>

</tasks>

<verification>
- All three theme files compile without errors
- DrawnTypography has 7+ custom TextStyle definitions with only weights 400 and 500
- DarkColorScheme includes all 25 M3 color slots
- DrawnCardElevation defined for consistent card shadows
- Full debug build succeeds
</verification>

<success_criteria>
- THEME-01: Dark mystical theme defined with purple (#9C27B0), gold (#FFD700), dark backgrounds (#121212, #1E1E1E)
- THEME-02: All M3 color slots populated, typography scale defined, card elevation standardized — foundation ready for all screens to consume
</success_criteria>

<output>
After completion, create `.planning/phases/03-enrichment-polish/03-01-SUMMARY.md`
</output>
