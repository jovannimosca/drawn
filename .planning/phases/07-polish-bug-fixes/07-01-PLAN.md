---
phase: 07-polish-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Reading list fades in when data loads"
    - "Fade animation triggers once when readings become available"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt"
      provides: "Reading list with fade-in animation"
      contains: "AnimatedVisibility.*visible.*listVisible"
  key_links:
    - from: "ReadingListScreen.kt"
      to: "ReadingListUiState.Success"
      via: "when branch checks uiState type"
      pattern: "is ReadingListUiState.Success"
---

<objective>
Fix AnimatedVisibility fade-in to trigger on state transition.

The audit identified that the reading list should fade in when data loads, but the animation never triggers because `visible = true` is hardcoded. Current code has `listVisible` variable tracking state, but it only transitions once and there's a reset issue on data refresh.
</objective>

<read_first>
- app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
- app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt
</read_first>

<context>
@.planning/phases/07-polish-bug-fixes/07-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Fix AnimatedVisibility fade-in to trigger on state transition</name>
  <files>app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt</files>
  <action>
Analyze the current AnimatedVisibility implementation and fix the fade-in to properly trigger when data loads:

1. Current state: `listVisible` is set to true once at line 142-143, but doesn't reset when data refreshes
2. The animation should trigger every time the list transitions from empty/loading to having data
3. Fix by ensuring the visibility state properly tracks whether to show the list content

Options to consider:
- Bind AnimatedVisibility visible parameter to check if readings are non-empty (simpler)
- Add a LaunchedEffect that resets listVisible when uiState changes to Loading, then sets to true when Success has data (more robust)

Recommend: The second approach ensures animation triggers on each data load, not just the first.
  </action>
  <verify>
<automated>grep -n "AnimatedVisibility" app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt | head -5</automated>
  </verify>
  <done>
- AnimatedVisibility visible parameter is bound to state that changes when data loads
- Fade-in animation triggers when readings become available
- Animation works on both initial load and subsequent refreshes
  </done>
  <acceptance_criteria>
- AnimatedVisibility at line ~167 uses dynamic visible state (not hardcoded true)
- Fade-in animation visibly triggers when reading list loads
- No regression: empty state and search state continue to work correctly
  </acceptance_criteria>
</task>

</tasks>

<verification>
Build the app to verify no compilation errors:
./gradlew assembleDebug
</verification>

<success_criteria>
Reading list fades in when data loads — animation triggers correctly on state transition
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-bug-fixes/07-01-SUMMARY.md`
</output>