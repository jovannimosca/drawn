---
phase: 02-core-recording-loop
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/addreading/CardAssignmentStep.kt
  - app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt
  - app/src/main/kotlin/com/example/drawn/ui/addreading/CardPickerBottomSheet.kt
  - app/src/main/kotlin/com/example/drawn/ui/addreading/CardThumbnail.kt
autonomous: true
requirements: [READ-01, CARD-01, CARD-02, CARD-03]

must_haves:
  truths:
    - "User sees all spread positions as tappable slots in Step 2"
    - "Tapping a slot opens a bottom sheet with all 78 cards sectioned by arcana type and suit"
    - "Tapping a card in the bottom sheet assigns it to the slot"
    - "Assigned slots show the card thumbnail and name"
    - "Unassigned slots show position name, meaning, and 'Tap to select a card' hint"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/ui/addreading/CardAssignmentStep.kt"
      provides: "Step 2 content showing all position slots for selected spread"
      exports: ["CardAssignmentStep"]
    - path: "app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt"
      provides: "Single position slot showing assigned card or unassigned placeholder"
      exports: ["PositionSlot"]
    - path: "app/src/main/kotlin/com/example/drawn/ui/addreading/CardPickerBottomSheet.kt"
      provides: "Bottom sheet with sectioned 78-card grid for card selection"
      exports: ["CardPickerBottomSheet"]
    - path: "app/src/main/kotlin/com/example/drawn/ui/addreading/CardThumbnail.kt"
      provides: "Reusable card image + name composable"
      exports: ["CardThumbnail"]
  key_links:
    - from: "app/src/main/kotlin/com/example/drawn/ui/addreading/CardAssignmentStep.kt"
      to: "app/src/main/kotlin/com/example/drawn/ui/addreading/AddReadingViewModel.kt"
      via: "assignCard callback passed from parent"
      pattern: "assignCard\\("
    - from: "app/src/main/kotlin/com/example/drawn/ui/addreading/CardPickerBottomSheet.kt"
      to: "app/src/main/kotlin/com/example/drawn/ui/addreading/CardAssignmentStep.kt"
      via: "modalBottomSheet shown when slot tapped"
      pattern: "ModalBottomSheet|BottomSheetScaffold"
---

<objective>
Build Step 2 (CardAssignment) — the core card-to-position assignment UI with bottom sheet card picker.

Purpose: This is the most complex screen in Phase 2 — users browse 78 cards and assign them to spread positions. Per D-16, D-17, D-18, D-19.
Output: Card assignment step with position slots, card picker bottom sheet, and card thumbnail component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-recording-loop/02-CONTEXT.md
@.planning/phases/02-core-recording-loop/02-UI-SPEC.md

@app/src/main/kotlin/com/example/drawn/ui/addreading/AddReadingViewModel.kt
@app/src/main/kotlin/com/example/drawn/ui/addreading/AddReadingState.kt
@app/src/main/kotlin/com/example/drawn/domain/model/Card.kt
@app/src/main/kotlin/com/example/drawn/domain/model/Spread.kt
@app/src/main/kotlin/com/example/drawn/domain/model/ArcanaType.kt
@app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From AddReadingState (Plan 01 output):
```kotlin
data class AddReadingState(
    val currentStep: AddReadingStep,
    val selectedSpread: Spread?,
    val cards: List<Card>,
    val assignedCards: Map<Int, Card>,  // positionOrder -> Card
    ...
)
```

From AddReadingViewModel (Plan 01 output):
```kotlin
fun assignCard(positionOrder: Int, card: Card)
fun unassignCard(positionOrder: Int)
```

From domain models:
```kotlin
data class Card(val id: Long, val deckId: Long, val name: String, val number: Int, val arcanaType: ArcanaType, val imageResId: Int, ...)
data class SpreadPosition(val name: String, val order: Int, val meaning: String)
enum class ArcanaType { MAJOR, MINOR }
```

From Color.kt:
```kotlin
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceVariant = Color(0xFF2C2C2C)
val DarkPrimary = Color(0xFF9C27B0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CardAssignmentStep and PositionSlot components</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/addreading/CardAssignmentStep.kt
    app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt
  </files>
  <action>
    1. Create PositionSlot.kt:
       - Signature: `fun PositionSlot(position: SpreadPosition, assignedCard: Card?, isAssigned: Boolean, onTap: () -> Unit, modifier: Modifier = Modifier)`
       - Two visual states per UI-SPEC Position Slot States table:
         - Unassigned: DarkSurfaceVariant (#2C2C2C) background, 1.dp dashed border, centered card icon placeholder (use Icons.Default.AddCircle), position name (titleMedium), position meaning (bodyMedium, onSurfaceVariant), "Tap to select a card" hint (labelSmall, onSurfaceVariant)
         - Assigned: DarkSurface (#1E1E1E) background, 1.dp solid DarkPrimary border, CardThumbnail showing assigned card, position name above card
       - Clickable with 48.dp minimum touch target
       - Content padding: 12.dp

    2. Create CardAssignmentStep.kt:
       - Signature: `fun CardAssignmentStep(spread: Spread, assignedCards: Map<Int, Card>, onCardTap: (positionOrder: Int) -> Unit, modifier: Modifier = Modifier)`
       - LazyColumn with spread position slots
       - Header: spread name (headlineSmall), 16.dp bottom padding
       - For each position in spread.positions (sorted by order): render PositionSlot
         - Pass assignedCards[position.order] as assignedCard
         - onTap calls onCardTap with position.order
       - 16.dp content padding, 12.dp spacing between slots
       - Show progress: "X of Y positions assigned" at top (bodyMedium, onSurfaceVariant)
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin --quiet 2>&1 | head -20</automated>
  </verify>
  <done>CardAssignmentStep shows all positions as tappable slots. Unassigned slots show placeholder, assigned slots show card thumbnail. Progress indicator shows assignment count.</done>
</task>

<task type="auto">
  <name>Task 2: Create CardPickerBottomSheet and CardThumbnail</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/addreading/CardPickerBottomSheet.kt
    app/src/main/kotlin/com/example/drawn/ui/addreading/CardThumbnail.kt
  </files>
  <action>
    1. Create CardThumbnail.kt:
       - Signature: `fun CardThumbnail(card: Card, modifier: Modifier = Modifier)`
       - Column with:
         - AsyncImage from Coil 3: model = card.imageResId (painterResource for drawable), contentDescription = card.name, contentScale = ContentScale.Crop, modifier = 80.dp width, aspect ratio ~2:3 (1.5f)
         - Card name below image: `titleMedium`, maxLines = 2, overflow = TextOverflow.Ellipsis, textAlign = TextAlign.Center
       - Horizontal center alignment
       - Per UI-SPEC: 80.dp width, 2:3 aspect ratio for tarot card proportions

    2. Create CardPickerBottomSheet.kt:
       - Signature: `fun CardPickerBottomSheet(cards: List<Card>, onCardSelected: (Card) -> Unit, onDismiss: () -> Unit)`
       - Use ModalBottomSheet from Material 3
       - Sheet content:
         - Header: "Select a Card" (titleMedium, bold), 16.dp padding
         - LazyVerticalGrid with 3 columns (per UI-SPEC Card Grid Layout)
         - Cards grouped by section with section headers (per D-18, D-19):
           - Section 1: "Major Arcana" — cards where arcanaType == MAJOR, sorted by number
           - Section 2: "Wands" — MINOR cards with "Wands" in name, sorted by number
           - Section 3: "Cups" — MINOR cards with "Cups" in name, sorted by number
           - Section 4: "Swords" — MINOR cards with "Swords" in name, sorted by number
           - Section 5: "Pentacles" — MINOR cards with "Pentacles" in name, sorted by number
         - Section headers: full-width, labelLarge typography, 16.dp top/bottom padding, onSurfaceVariant color
         - Grid spacing: 8.dp horizontal, 12.dp vertical
         - Each card: CardThumbnail, clickable → calls onCardSelected(card) then onDismiss
       - Per UI-SPEC: grid spacing 8.dp horizontal, 12.dp vertical
       - Use stickyHeader for section headers in LazyVerticalGrid

    3. Wire into CardAssignmentStep:
       - CardAssignmentStep needs to accept an additional parameter: `showCardPicker: Boolean` and `onCardPickerDismiss: () -> Unit`
       - OR: CardAssignmentStep manages bottom sheet state internally with `rememberModalBottomSheetState()` and exposes `onCardSelected` callback
       - Recommended: CardAssignmentStep takes `onPositionCardSelected: (positionOrder: Int, Card) -> Unit` — when user taps slot, opens bottom sheet; when card selected, calls callback with positionOrder + card
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin --quiet 2>&1 | head -20</automated>
  </verify>
  <done>CardPickerBottomSheet shows all 78 cards in 5 sections (Major Arcana, Wands, Cups, Swords, Pentacles) with 3-column grid. CardThumbnail renders card image + name. Tapping a card dismisses sheet and assigns card.</done>
</task>

</tasks>

<verification>
- CardAssignmentStep renders all positions for the selected spread
- PositionSlot shows different visuals for assigned vs unassigned states
- CardPickerBottomSheet opens when tapping a position slot
- Cards are sectioned: Major Arcana first, then Wands, Cups, Swords, Pentacles
- Within each section, cards ordered by number (Ace through King)
- CardThumbnail shows image (80.dp width, 2:3 ratio) + card name
- Tapping a card assigns it to the position and dismisses the bottom sheet
- Grid uses 3 columns with correct spacing (8.dp horizontal, 12.dp vertical)
- Code compiles without errors
</verification>

<success_criteria>
- User can see all spread positions as tappable slots in Step 2
- Tapping a slot opens the card picker bottom sheet
- All 78 cards are visible in the picker, organized by arcana type and suit
- Tapping a card assigns it to the position
- Assigned positions show the card thumbnail
- Unassigned positions show position name, meaning, and tap hint
- UI matches UI-SPEC design contract for position slot states and card grid layout
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-recording-loop/02-03-SUMMARY.md`
</output>
