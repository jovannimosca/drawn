---
phase: 02-core-recording-loop
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt
  - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
  - app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt
  - app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailViewModel.kt
autonomous: true
requirements: [READ-03, READ-04]

must_haves:
  truths:
    - "FAB on ReadingList navigates to AddReading wizard"
    - "After saving a reading, user returns to ReadingList and sees the new reading"
    - "Tapping a reading in the list shows full detail with spread layout, assigned cards, and notes"
    - "Reading detail shows cards in position order with card thumbnails"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt"
      provides: "AddReading NavEntry wired to AddReadingScreen, ReadingDetail wired to ReadingDetailScreen"
      contains: "is AddReading -> NavEntry"
    - path: "app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt"
      provides: "Full reading detail view with spread layout, cards, notes"
      exports: ["ReadingDetailScreen"]
    - path: "app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailViewModel.kt"
      provides: "ViewModel loading reading detail via observeReadingWithDetails"
      exports: ["ReadingDetailViewModel", "ReadingDetailUiState"]
  key_links:
    - from: "app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt"
      to: "app/src/main/kotlin/com/example/drawn/ui/addreading/AddReadingScreen.kt"
      via: "NavEntry rendering AddReadingScreen"
      pattern: "AddReadingScreen\\("
    - from: "app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailViewModel.kt"
      to: "app/src/main/kotlin/com/example/drawn/data/repository/ReadingRepository.kt"
      via: "observeReadingWithDetails(id) call"
      pattern: "observeReadingWithDetails"
---

<objective>
Wire navigation between ReadingList → AddReading → back, and implement ReadingDetailScreen to view saved readings.

Purpose: Complete the user journey — create readings via the wizard, see them in the list, and view full details. Addresses remaining stubs from Phase 1.
Output: Wired navigation, ReadingDetailScreen with ViewModel, FAB navigation on ReadingList.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-recording-loop/02-CONTEXT.md
@.planning/phases/02-core-recording-loop/02-UI-SPEC.md
@.planning/phases/01-foundation-dev-environment/01-05-SUMMARY.md

@app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt
@app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
@app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt
@app/src/main/kotlin/com/example/drawn/data/repository/ReadingRepository.kt
@app/src/main/kotlin/com/example/drawn/domain/model/ReadingDetail.kt

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From DrawnNavHost.kt (existing — to be modified):
```kotlin
fun DrawnNavHost(modifier: Modifier = Modifier)
```
→ Currently has TODO for AddReading and ReadingDetail. Need to wire both.
→ ReadingListViewModel is injected at Activity level and passed down.
→ AddReadingViewModel will use hiltViewModel() inside its NavEntry.

From ReadingRepository.kt:
```kotlin
fun observeReadingWithDetails(id: Long): Flow<ReadingDetail?>
```
→ Returns ReadingDetail? (reading + cards + photos) or null if not found.

From domain models:
```kotlin
data class ReadingDetail(val reading: Reading, val cards: List<ReadingCard>, val photos: List<ReadingPhoto>)
data class ReadingCard(val id: Long, val readingId: Long, val cardId: Long, val positionName: String, val positionOrder: Int, val interpretation: String? = null, val isReversed: Boolean = false)
```

From ReadingListScreen.kt (existing pattern):
```kotlin
fun ReadingListScreen(
    viewModel: ReadingListViewModel = hiltViewModel(),
    onAddReading: () -> Unit,
    onReadingSelected: (Long) -> Unit
)
```
→ onAddReading currently wired to empty callback — needs backStack.add(AddReading)
→ onReadingSelected already wired to backStack.add(ReadingDetail(readingId))

From 01-05-SUMMARY.md (known stubs):
- ReadingDetail NavEntry shows placeholder text "Reading detail: {id}"
- AddReading callback is empty TODO
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AddReading navigation and implement ReadingDetailScreen</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt
    app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt
    app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailViewModel.kt
  </files>
  <action>
    1. Wire AddReading in DrawnNavHost.kt:
       - Replace TODO in onAddReading callback with: `backStack.add(AddReading)`
       - Add NavEntry for AddReading:
         ```kotlin
         is AddReading -> NavEntry(key) {
             AddReadingScreen(
                 onNavigateBack = { backStack.removeLastOrNull() }
             )
         }
         ```
       - Add import for AddReadingScreen and AddReading route

    2. Create ReadingDetailViewModel.kt:
       - @HiltViewModel with ReadingRepository injected
       - `val uiState: StateFlow<ReadingDetailUiState>` with:
         - `data object Loading`
         - `data class Success(val detail: ReadingDetail)` 
         - `data class Error(val message: String)`
       - Constructor takes readingId: Long parameter
       - Collect from readingRepository.observeReadingWithDetails(readingId)
       - Map to uiState: null reading → Error("Reading not found"), else Success
       - Follow ReadingListViewModel pattern (StateFlow + catch + stateIn)

    3. Create ReadingDetailScreen.kt:
       - Signature: `fun ReadingDetailScreen(readingId: Long, viewModel: ReadingDetailViewModel = hiltViewModel(), onNavigateBack: () -> Unit)`
       - Scaffold with:
         - TopAppBar: reading title (dynamic from state), back arrow calling onNavigateBack
         - Content: scrollable Column
       - Collect uiState via collectAsStateWithLifecycle()
       - Handle states:
         - Loading: Centered CircularProgressIndicator
         - Success(detail): Show content
         - Error: "Something went wrong" + error message (consistent with ReadingListScreen pattern)
       - Success content (per UI-SPEC):
         - Spread name as section header (headlineSmall) — need to derive from reading or show "Reading"
         - Cards in position order: for each ReadingCard, show PositionSlot-like display with position name + CardThumbnail + card name
         - Notes section (if present): bodyLarge text, 16.dp top padding
         - Date: bodyMedium in onSurfaceVariant, formatted as "MMM d, yyyy"
       - Note: For card thumbnails in detail view, we need card images. Since ReadingCard only has cardId, we need to either:
         - Option A: Pass card images via a separate lookup (CardRepository.observeCardById)
         - Option B: Show card name + position only (simpler, no extra repository call)
         - Use Option B for now — show position name + card name text. Card images can be added when photos are implemented in Phase 3.
         - Actually: ReadingDetail only has ReadingCard with cardId, not the full Card. To show thumbnails, we'd need to join with Card table. Keep it simple: show position name and card name text.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin --quiet 2>&1 | head -20</automated>
  </verify>
  <done>AddReading navigates from FAB. ReadingDetailScreen shows reading with positions, card names, notes, and date. Back arrow returns to list.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ReadingDetail in NavHost and verify full flow</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt
  </files>
  <action>
    1. Replace ReadingDetail placeholder in DrawnNavHost.kt:
       - Current: `Text(text = "Reading detail: ${key.readingId}")`
       - New: `ReadingDetailScreen(readingId = key.readingId, onNavigateBack = { backStack.removeLastOrNull() })`
       - Add import for ReadingDetailScreen

    2. Verify the full navigation graph compiles:
       - ReadingList → AddReading (FAB)
       - AddReading → ReadingList (back arrow or save completion)
       - ReadingList → ReadingDetail (item tap)
       - ReadingDetail → ReadingList (back arrow)

    3. Ensure AddReadingScreen handles Saved state by calling onNavigateBack (which removes AddReading from backstack, returning to ReadingList)
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin --quiet 2>&1 | head -20</automated>
  </verify>
  <done>Full navigation graph compiles with 3 routes (ReadingList, AddReading, ReadingDetail). All TODO stubs from Phase 1 are resolved. Navigation flows work in both directions.</done>
</task>

</tasks>

<verification>
- FAB on ReadingList navigates to AddReading wizard
- AddReadingScreen shows when AddReading route is active
- Back arrow in AddReading returns to ReadingList
- Saved state in AddReadingViewModel triggers navigation back to ReadingList
- ReadingDetailScreen replaces placeholder text in NavHost
- Tapping a reading in the list shows ReadingDetailScreen with reading data
- ReadingDetailViewModel loads data via observeReadingWithDetails
- All 3 routes (ReadingList, AddReading, ReadingDetail) compile and wire correctly
- No TODO stubs remain in DrawnNavHost.kt
</verification>

<success_criteria>
- Complete user journey: FAB → wizard → save → list shows new reading → tap → detail view
- ReadingDetailScreen displays spread name, position-card pairs, notes, and date
- All navigation stubs from Phase 1 are replaced with real screens
- Code compiles without errors
- No remaining TODO comments in DrawnNavHost.kt
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-recording-loop/02-04-SUMMARY.md`
</output>
