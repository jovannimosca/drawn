---
phase: 06
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
  - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "Reading list cards show date and spread name as metadata alongside title"
    - "Reading list cards are compact — more readings visible per screen"
    - "Notes preview is truncated to max 2 lines with ellipsis"
    - "Reading list fades in when data loads"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt"
      provides: "Updated ReadingListItem with date, spread, compact layout, fade-in animation"
      contains: "AnimatedVisibility"
  key_links:
    - from: "app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt"
      to: "Reading domain model"
      via: "Reading.createdAt and spreadId access"
      pattern: "reading\\.(createdAt|spreadId|spreadName)"
---

<objective>
Compact reading list cards with metadata (date + spread name) and fade-in loading animation.

Purpose: Per D-15, reading list cards must be compact showing title, date, spread name, and notes preview (max 2 lines). Per D-05, reading list uses subtle fade-in when loading data. This replaces the current minimal card (title + optional notes only) with a richer but denser card layout.
Output: Updated ReadingListItem with metadata, compact spacing, and fade-in animation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refine-ui-theming-and-screens/06-CONTEXT.md
@app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
@app/src/main/kotlin/com/example/drawn/domain/model/Reading.kt

<interfaces>
From app/src/main/kotlin/com/example/drawn/domain/model/Reading.kt:
```kotlin
data class Reading(
    val id: Long,
    val title: String,
    val notes: String?,
    val spreadId: Long,
    val createdAt: Instant,
    val updatedAt: Instant
)
```

From app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt (current ReadingListItem, lines 241-268):
```kotlin
@Composable
private fun ReadingListItem(
    reading: Reading,
    onClick: () -> Unit,
    modifier: Modifier = Modifier
) {
    Card(onClick = onClick, modifier = modifier.padding(vertical = 4.dp)) {
        Column(modifier = Modifier.padding(16.dp)) {
            Text(text = reading.title, style = MaterialTheme.typography.titleMedium)
            reading.notes?.let { notes ->
                Spacer(modifier = Modifier.height(4.dp))
                Text(text = notes, style = MaterialTheme.typography.bodyMedium,
                     color = MaterialTheme.colorScheme.onSurfaceVariant, maxLines = 2)
            }
        }
    }
}
```

From ReadingListViewModel — need to check if spreadName is available:
```kotlin
// ReadingListUiState.Success currently provides List<Reading>
// May need to be updated to include spreadName (JOIN query) or use spreadId lookup
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spread name to ReadingListViewModel and update ReadingListItem with metadata</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt
    app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
  </files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt (current state, how readings are loaded)
    - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt (current ReadingListItem, lines 241-268)
    - app/src/main/kotlin/com/example/drawn/domain/model/Reading.kt (Reading model with spreadId)
    - app/src/main/kotlin/com/example/drawn/data/repository/ReadingRepository.kt (how readings are fetched)
  </read_first>
  <action>
    **Step A — Check if spread name is available in the current data flow:**

    Read ReadingListViewModel.kt and ReadingRepository.kt to determine how readings are loaded. The Reading model has `spreadId: Long` but no `spreadName`. Per STATE.md decision: "Filter against title and notes only — Reading model has spreadId (Long) not spreadName (String), so spread name text search is not possible without a JOIN."

    If the repository already provides spread name (e.g., via a JOIN or a separate model like `ReadingWithSpread`), use that. If not, create a lightweight approach:
    - Option 1: Add a `ReadingWithSpread` data class with an additional `spreadName: String` field
    - Option 2: Add a DAO query that JOINs spreads table and returns the name
    - Pick whichever requires fewer changes — the goal is to display spread name, not rearchitect the data layer

    **Step B — Update ReadingListItem (D-15: compact cards with metadata):**

    Replace the current ReadingListItem with a compact layout showing:
    - **Row 1:** `reading.title` in `titleMedium` (bold), with `reading.createdAt` formatted as "MMM d, yyyy" in `labelSmall` with `onSurfaceVariant` color, right-aligned
    - **Row 2:** Spread name (e.g., "Celtic Cross") in `labelMedium` with `DarkSecondary` (gold) tint
    - **Row 3:** Notes preview (if present) in `bodyMedium` with `onSurfaceVariant`, `maxLines = 2`, `overflow = TextOverflow.Ellipsis`

    Spacing: 12.dp horizontal padding (reduced from 16.dp for compactness), 4.dp vertical padding on Card, 2.dp between rows.

    Use `Row` with `Arrangement.SpaceBetween` for title + date on the same line.

    **Step C — Add fade-in animation (D-05):**

    Wrap the LazyColumn in `AnimatedVisibility(visible = readings.isNotEmpty(), enter = fadeIn())` or apply `Modifier.fadeIn()` to individual items. Since local Room DB is fast, use a simple `AnimatedVisibility` with `fadeIn(animationSpec = tween(durationMillis = 300))` around the ReadingList composable.

    Import `androidx.compose.animation.AnimatedVisibility` and `androidx.compose.animation.fadeIn` from `androidx.compose.animation` (already in dependencies per build.gradle.kts line 91: `implementation(libs.androidx.compose.animation)`).
  </action>
  <acceptance_criteria>
    - ReadingListItem shows title + formatted date on same row (date right-aligned, labelSmall, onSurfaceVariant)
    - ReadingListItem shows spread name below title (labelMedium, gold/DarkSecondary color)
    - ReadingListItem notes preview uses maxLines = 2 with TextOverflow.Ellipsis
    - ReadingListItem uses 12.dp horizontal padding (not 16.dp)
    - Card vertical padding is 4.dp
    - ReadingList is wrapped in AnimatedVisibility with fadeIn animation
    - `import androidx.compose.animation.AnimatedVisibility` present in ReadingListScreen.kt
    - `import androidx.compose.animation.fadeIn` present in ReadingListScreen.kt
    - Date formatted using DateTimeFormatter with pattern "MMM d, yyyy"
    - `./gradlew :app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>ReadingListItem shows title+date, spread name, notes preview (2 lines max) in compact layout; list fades in on load; project compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reading data → UI | Reading title and notes are user-entered text — displayed as-is in Compose Text composables (safe, no HTML injection risk in Compose) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | ReadingListItem | accept | All data is local-only, user's own readings. No cross-user data exposure. |
| T-06-04 | Injection | ReadingListItem text display | accept | Compose Text composable renders plain text only — no HTML/JS injection possible. |
</threat_model>

<verification>
- ReadingListItem displays title, date, spread name, and notes preview
- Date is formatted as "MMM d, yyyy" (e.g., "Apr 5, 2026")
- Spread name appears in gold color (DarkSecondary)
- Notes truncated to 2 lines with ellipsis
- Card padding is compact (12.dp horizontal, 4.dp vertical)
- AnimatedVisibility with fadeIn wraps the reading list
- Project compiles without errors
</verification>

<success_criteria>
- Reading list cards show: title + date (row 1), spread name (row 2), notes preview (row 3, max 2 lines)
- Cards are visually compact — more readings fit per screen
- Reading list fades in when data loads (300ms fadeIn)
- All text uses correct typography scale per D-18
- Project compiles and builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06-refine-ui-theming-and-screens/06-02-SUMMARY.md`
</output>
