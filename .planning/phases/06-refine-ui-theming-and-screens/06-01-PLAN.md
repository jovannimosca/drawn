---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/components/EmptyState.kt
  - app/src/main/kotlin/com/example/drawn/ui/components/ErrorBanner.kt
  - app/src/main/kotlin/com/example/drawn/ui/components/GoldDivider.kt
  - app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt
  - app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt
  - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "Shared EmptyState composable exists and is themed with mystical aesthetics"
    - "Shared ErrorBanner composable exists as a themed replacement for inline error cards"
    - "GoldDivider composable renders a thin gold/amber line between sections"
    - "ReadingListScreen uses EmptyState instead of inline private composable"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/ui/components/EmptyState.kt"
      provides: "Reusable themed empty state composable"
      exports: ["EmptyState"]
      min_lines: 20
    - path: "app/src/main/kotlin/com/example/drawn/ui/components/ErrorBanner.kt"
      provides: "Reusable themed error banner composable"
      exports: ["ErrorBanner"]
      min_lines: 20
    - path: "app/src/main/kotlin/com/example/drawn/ui/components/GoldDivider.kt"
      provides: "Gold accent divider composable"
      exports: ["GoldDivider"]
      min_lines: 10
    - path: "app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt"
      provides: "Updated to use shared EmptyState"
      contains: "import com.example.drawn.ui.components.EmptyState"
  key_links:
    - from: "app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt"
      to: "app/src/main/kotlin/com/example/drawn/ui/components/EmptyState.kt"
      via: "import and usage replacing inline EmptyState"
      pattern: "EmptyState\\(modifier"
---

<objective>
Create shared UI components (EmptyState, ErrorBanner, GoldDivider) and migrate ReadingListScreen to use them.

Purpose: Per D-19, new shared components go in `ui/components/`. Currently, empty states and error cards are duplicated as private composables in each screen. This plan extracts them into reusable, themed components and adds a gold divider for section separation (D-13).
Output: Three new shared components + ReadingListScreen migration
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refine-ui-theming-and-screens/06-CONTEXT.md
@app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
@app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt
@app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt

<interfaces>
From app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt:
```kotlin
val DarkSecondary = Color(0xFFFFD700) // Gold accent
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceVariant = Color(0xFF2C2C2C)
val DarkError = Color(0xFFCF6679)
val DarkErrorContainer = Color(0xFF93000A)
val DarkOnErrorContainer = Color(0xFFFFDAD6)
```

From app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt:
```kotlin
// Current inline EmptyState (lines 192-219) — to be replaced
@Composable private fun EmptyState(modifier: Modifier = Modifier) { ... }

// Current inline ErrorCard (lines 154-189) — to be replaced
@Composable private fun ErrorCard(message: String, modifier: Modifier = Modifier) { ... }

// ReadingListScreen signature
@Composable fun ReadingListScreen(
    viewModel: ReadingListViewModel = hiltViewModel(),
    onAddReading: () -> Unit,
    onReadingSelected: (Long) -> Unit
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared UI components (EmptyState, ErrorBanner, GoldDivider)</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/components/EmptyState.kt
    app/src/main/kotlin/com/example/drawn/ui/components/ErrorBanner.kt
    app/src/main/kotlin/com/example/drawn/ui/components/GoldDivider.kt
  </files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt (current inline EmptyState and ErrorCard implementations)
    - app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt (color tokens)
  </read_first>
  <action>
    Create the `ui/components/` directory (does not exist yet per D-19) and three shared composables:

    **EmptyState.kt** — Extract from ReadingListScreen's inline EmptyState (lines 192-219). Make it reusable with parameters:
    - `icon: String = "✨"` (default sparkle for mystical aesthetic)
    - `title: String` (required — e.g., "No readings yet")
    - `message: String` (required — e.g., "Tap + to record your first reading")
    - `modifier: Modifier = Modifier`
    - Uses `displayLarge` for icon, `headlineSmall` for title, `bodyLarge` with `onSurfaceVariant` for message
    - Centered Column with 32.dp padding, 16.dp and 8.dp spacers between elements

    **ErrorBanner.kt** — Extract from ReadingListScreen's inline ErrorCard (lines 154-189). Make it reusable:
    - `message: String` (required)
    - `title: String = "Couldn't load readings"` (default, overridable)
    - `modifier: Modifier = Modifier`
    - Uses Card with `errorContainer` / `onErrorContainer` colors
    - Warning icon (Icons.Default.Warning) at 48.dp, tinted with `error` color
    - Title in `titleMedium`, message in `bodyMedium` with `textAlign = TextAlign.Center`
    - 16.dp padding, 8.dp and 4.dp spacers

    **GoldDivider.kt** — New composable for D-13 (gold accent dividers between sections):
    - `modifier: Modifier = Modifier`
    - `thickness: Dp = 1.dp`
    - `color: Color = DarkSecondary` (gold accent, 0xFFFFD700)
    - Renders a `Divider` (from `androidx.compose.material3.Divider`) with the gold color
    - Import `androidx.compose.material3.Divider` and `androidx.compose.material3.MaterialTheme`
  </action>
  <acceptance_criteria>
    - File `app/src/main/kotlin/com/example/drawn/ui/components/EmptyState.kt` exists with `@Composable fun EmptyState` exported
    - EmptyState has parameters: icon (String, default "✨"), title (String), message (String), modifier
    - File `app/src/main/kotlin/com/example/drawn/ui/components/ErrorBanner.kt` exists with `@Composable fun ErrorBanner` exported
    - ErrorBanner has parameters: message (String), title (String, default "Couldn't load readings"), modifier
    - File `app/src/main/kotlin/com/example/drawn/ui/components/GoldDivider.kt` exists with `@Composable fun GoldDivider` exported
    - GoldDivider uses DarkSecondary (0xFFFFD700) as default color, 1.dp default thickness
    - All three files compile (no syntax errors)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>Three shared component files exist in ui/components/, each exports a @Composable function with correct parameters, project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ReadingListScreen to use shared EmptyState and ErrorBanner</name>
  <files>app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt (current state)
    - app/src/main/kotlin/com/example/drawn/ui/components/EmptyState.kt (just created)
    - app/src/main/kotlin/com/example/drawn/ui/components/ErrorBanner.kt (just created)
  </read_first>
  <action>
    Update ReadingListScreen.kt to replace inline composables with shared components:

    1. Add imports:
       - `import com.example.drawn.ui.components.EmptyState`
       - `import com.example.drawn.ui.components.ErrorBanner`
       - Remove unused imports: `Icons.Default.Warning` (no longer needed inline)

    2. Replace inline `ErrorCard` composable (lines 154-189) — delete the entire private function. Replace its usage at line 116-119:
       ```kotlin
       ErrorBanner(message = message, modifier = Modifier.padding(16.dp))
       ```

    3. Replace inline `EmptyState` composable (lines 192-219) — delete the entire private function. Replace its usage at line 140:
       ```kotlin
       EmptyState(
           title = "No readings yet",
           message = "Tap + to record your first reading",
           modifier = Modifier.fillMaxSize()
       )
       ```

    4. Keep the search-no-results Text as-is (it's a different case — not the main empty state)

    5. Verify the file still compiles and all references resolve
  </action>
  <acceptance_criteria>
    - ReadingListScreen.kt contains `import com.example.drawn.ui.components.EmptyState`
    - ReadingListScreen.kt contains `import com.example.drawn.ui.components.ErrorBanner`
    - ReadingListScreen.kt does NOT contain `private fun ErrorCard` (deleted)
    - ReadingListScreen.kt does NOT contain `private fun EmptyState` (deleted)
    - ReadingListScreen.kt calls `EmptyState(` with title and message parameters
    - ReadingListScreen.kt calls `ErrorBanner(` with message parameter
    - `./gradlew :app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>ReadingListScreen uses shared EmptyState and ErrorBanner, inline private composables removed, project compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI component parameters | String inputs (title, message, icon) — no user input crosses trust boundary, all are developer-defined strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Injection | EmptyState icon parameter | accept | Icon is a developer-defined string parameter, not user input. No injection risk. |
| T-06-02 | Tampering | GoldDivider color | accept | Color is a compile-time constant (DarkSecondary), not runtime-configurable. |
</threat_model>

<verification>
- All three component files exist in `ui/components/`
- Each exports a properly parameterized @Composable function
- ReadingListScreen imports and uses EmptyState and ErrorBanner
- Inline private ErrorCard and EmptyState functions removed from ReadingListScreen
- Project compiles without errors
</verification>

<success_criteria>
- `ui/components/` directory created with 3 files
- EmptyState renders sparkle icon + title + message, centered
- ErrorBanner renders warning icon + title + message in error-colored card
- GoldDivider renders 1dp gold line
- ReadingListScreen compiles and uses shared components
- No duplicate empty state or error card implementations remain in ReadingListScreen
</success_criteria>

<output>
After completion, create `.planning/phases/06-refine-ui-theming-and-screens/06-01-SUMMARY.md`
</output>
