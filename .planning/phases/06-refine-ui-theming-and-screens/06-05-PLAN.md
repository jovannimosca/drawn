---
phase: 06
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt
  - app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt
  - app/src/main/kotlin/com/example/drawn/ui/components/GoldDivider.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "Reversed/upright toggle uses a 180° Y-axis flip animation (not instant rotation)"
    - "Flip animation is visually satisfying — smooth transition between states"
    - "ReadingDetailScreen card items also use flip animation for reversed toggle"
    - "GoldDivider composable is used between sections in ReadingDetailScreen"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt"
      provides: "PositionSlot with animated Y-axis flip for reversed toggle"
      contains: "animateFloatAsState"
    - path: "app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt"
      provides: "ReadingDetailCardItem with flip animation + GoldDivider between sections"
      contains: "animateFloatAsState"
  key_links:
    - from: "app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt"
      to: "Compose Animation API"
      via: "animateFloatAsState for rotationY"
      pattern: "animateFloatAsState.*rotationY"
---

<objective>
Add 180° Y-axis flip animation to reversed card toggles and gold dividers to ReadingDetailScreen.

Purpose: Per D-03, reversed/upright card toggle uses 180° Y-axis flip animation — visually satisfying feedback. Currently uses static `rotationZ = if (isReversed) 180f else 0f` via `graphicsLayer`. Per D-13, gold accent dividers separate sections in ReadingDetailScreen.
Output: Animated flip in PositionSlot and ReadingDetailCardItem, gold dividers in ReadingDetailScreen
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-refine-ui-theming-and-screens/06-CONTEXT.md
@app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt
@app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt
@app/src/main/kotlin/com/example/drawn/ui/components/GoldDivider.kt

<interfaces>
From app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt (current reversed implementation, lines 112-114):
```kotlin
CardThumbnail(
    card = card,
    modifier = Modifier.graphicsLayer {
        rotationZ = if (isReversed) 180f else 0f
    }
)
```

From app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt (current reversed implementation, lines 615-617):
```kotlin
CardThumbnail(
    card = card,
    modifier = Modifier.graphicsLayer {
        rotationZ = if (readingCard.isReversed) 180f else 0f
    }
)
```

Both use rotationZ (Z-axis rotation = 2D spin). D-03 requires Y-axis flip (3D card flip effect).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Y-axis flip animation to PositionSlot reversed toggle</name>
  <files>app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/addreading/PositionSlot.kt (current rotationZ implementation)
  </read_first>
  <action>
    Replace the static `rotationZ` with an animated Y-axis flip in PositionSlot.kt:

    1. Add imports:
       - `import androidx.compose.animation.core.animateFloatAsState`
       - `import androidx.compose.animation.core.tween`

    2. In the `AssignedSlotContent` composable (line 89), replace the static rotation with animated flip:
       ```kotlin
       val flipAngle by animateFloatAsState(
           targetValue = if (isReversed) 180f else 0f,
           animationSpec = tween(durationMillis = 400),
           label = "cardFlip"
       )
       ```

    3. Update the CardThumbnail modifier from:
       ```kotlin
       Modifier.graphicsLayer {
           rotationZ = if (isReversed) 180f else 0f
       }
       ```
       To:
       ```kotlin
       Modifier.graphicsLayer {
           rotationY = flipAngle
           cameraDistance = 12f * density  // Add perspective for 3D effect
       }
       ```

    Key details:
    - Use `rotationY` (not `rotationZ`) for the Y-axis flip effect
    - Set `cameraDistance = 12f * density` for perspective — default is too far away, 12f gives a satisfying 3D flip
    - Animation duration: 400ms — fast enough to feel responsive, slow enough to see the flip
    - The `isReversed` parameter is passed from the parent, so the animation triggers when the parent toggles the state
  </action>
  <acceptance_criteria>
    - PositionSlot.kt contains `import androidx.compose.animation.core.animateFloatAsState`
    - PositionSlot.kt contains `import androidx.compose.animation.core.tween`
    - `animateFloatAsState` used with targetValue `if (isReversed) 180f else 0f`
    - Animation spec uses `tween(durationMillis = 400)`
    - `graphicsLayer` uses `rotationY = flipAngle` (not rotationZ)
    - `cameraDistance = 12f * density` set in graphicsLayer for 3D perspective
    - `./gradlew :app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>PositionSlot uses animated Y-axis flip with 400ms tween and cameraDistance for 3D perspective, compiles successfully</done>
</task>

<task type="auto">
  <name>Task 2: Add flip animation to ReadingDetailCardItem and gold dividers to ReadingDetailScreen</name>
  <files>app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt</files>
  <read_first>
    - app/src/main/kotlin/com/example/drawn/ui/readingdetail/ReadingDetailScreen.kt (current ReadingDetailCardItem, lines 587-629)
    - app/src/main/kotlin/com/example/drawn/ui/components/GoldDivider.kt (created in Plan 01)
  </read_first>
  <action>
    **Part A — Flip animation in ReadingDetailCardItem:**

    1. Add imports:
       - `import androidx.compose.animation.core.animateFloatAsState`
       - `import androidx.compose.animation.core.tween`

    2. In `ReadingDetailCardItem` composable (line 587), add animated flip:
       ```kotlin
       val flipAngle by animateFloatAsState(
           targetValue = if (readingCard.isReversed) 180f else 0f,
           animationSpec = tween(durationMillis = 400),
           label = "detailCardFlip"
       )
       ```

    3. Update the CardThumbnail modifier from:
       ```kotlin
       Modifier.graphicsLayer {
           rotationZ = if (readingCard.isReversed) 180f else 0f
       }
       ```
       To:
       ```kotlin
       Modifier.graphicsLayer {
           rotationY = flipAngle
           cameraDistance = 12f * density
       }
       ```

    **Part B — Gold dividers between sections in ReadingDetailContent:**

    1. Add import: `import com.example.drawn.ui.components.GoldDivider`

    2. In `ReadingDetailContent` LazyColumn, add `GoldDivider()` between major sections:
       - After the spread name + title section (before "Cards" header) — add `GoldDivider(modifier = Modifier.padding(vertical = 8.dp))`
       - After the cards section (before "Notes" header) — add `GoldDivider(modifier = Modifier.padding(vertical = 8.dp))`
       - After the notes section (before "Photos" header) — add `GoldDivider(modifier = Modifier.padding(vertical = 8.dp))`

    3. Each GoldDivider item in the LazyColumn:
       ```kotlin
       item {
           GoldDivider(modifier = Modifier.padding(vertical = 8.dp))
       }
       ```
  </action>
  <acceptance_criteria>
    - ReadingDetailScreen.kt contains `import androidx.compose.animation.core.animateFloatAsState`
    - ReadingDetailScreen.kt contains `import androidx.compose.animation.core.tween`
    - ReadingDetailCardItem uses `animateFloatAsState` with targetValue `if (readingCard.isReversed) 180f else 0f`
    - ReadingDetailCardItem `graphicsLayer` uses `rotationY = flipAngle` (not rotationZ)
    - ReadingDetailCardItem sets `cameraDistance = 12f * density`
    - ReadingDetailScreen.kt contains `import com.example.drawn.ui.components.GoldDivider`
    - ReadingDetailContent LazyColumn has at least 3 `GoldDivider(` calls between sections
    - `./gradlew :app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>ReadingDetailCardItem uses animated Y-axis flip, ReadingDetailScreen has gold dividers between sections, project compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Animation state → UI | isReversed is a boolean from domain model — no user input crosses boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Denial of Service | animateFloatAsState | accept | Compose animation is GPU-accelerated, 400ms duration. No performance risk. |
| T-06-10 | Tampering | cameraDistance value | accept | Compile-time constant (12f), not runtime-configurable. |
</threat_model>

<verification>
- PositionSlot uses animateFloatAsState with rotationY (not rotationZ)
- PositionSlot sets cameraDistance = 12f * density for 3D perspective
- ReadingDetailCardItem uses same animated flip pattern
- Animation duration is 400ms with tween
- ReadingDetailScreen has GoldDivider between Cards/Notes/Photos sections
- GoldDivider uses DarkSecondary (gold) color
- Project compiles without errors
</verification>

<success_criteria>
- Double-clicking a position slot triggers a smooth 180° Y-axis flip animation
- Flip has 3D perspective effect (cameraDistance set)
- Animation completes in ~400ms
- ReadingDetailScreen cards also flip when isReversed changes
- Gold dividers visually separate sections in ReadingDetailScreen
- Project compiles and builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06-refine-ui-theming-and-screens/06-05-SUMMARY.md`
</output>
