---
phase: 09-repository-layer
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - app/src/main/kotlin/com/example/drawn/data/repository/ReadingRepository.kt
  - app/src/main/kotlin/com/example/drawn/data/database/dao/ReadingDao.kt
  - app/src/main/kotlin/com/example/drawn/data/database/dao/ReadingTagDao.kt
  - app/src/main/kotlin/com/example/drawn/di/RepositoryModule.kt
autonomous: true
requirements:
  - TAG-04
  - TAG-05
  - TAG-06
  - TAG-07
---

<objective>
Extend ReadingRepository with favorites and tag filtering capabilities.
</objective>

<context>
@app/src/main/kotlin/com/example/drawn/data/repository/ReadingRepository.kt
@app/src/main/kotlin/com/example/drawn/data/database/dao/ReadingDao.kt
@app/src/main/kotlin/com/example/drawn/data/database/dao/ReadingTagDao.kt
@app/src/main/kotlin/com/example/drawn/data/repository/ReadingTagRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add favorites observe methods to ReadingDao</name>
  <files>app/src/main/kotlin/com/example/drawn/data/database/dao/ReadingDao.kt</files>
  <action>
Add new query methods to ReadingDao (line ~30):
- @Query("SELECT * FROM readings WHERE isFavorite = 1 ORDER BY createdAt DESC")
  fun observeFavorites(): Flow<List<ReadingEntity>>
- Query same fields as observeAllReadingsWithSpread() for consistency (per D-02):
  @Query("""
      SELECT r.id, r.title, r.spreadId, s.name as spreadName, r.createdAt, r.notes, r.isFavorite
      FROM readings r
      INNER JOIN spreads s ON r.spreadId = s.id
      WHERE r.isFavorite = 1
      ORDER BY r.createdAt DESC
  """)
  fun observeFavoritesWithSpread(): Flow<List<ReadingWithSpread>>
  </action>
  <verify>
  <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>ReadingDao has observeFavorites and observeFavoritesWithSpread methods</done>
</task>

<task type="auto">
  <name>Task 2: Extend ReadingRepository with favorites</name>
  <files>app/src/main/kotlin/com/example/drawn/data/repository/ReadingRepository.kt</files>
  <action>
Add methods to ReadingRepository (after addPhotoToReading/delete methods):
- toggleFavorite(readingId: Long): Unit — suspend function, reads reading via readingDao.getReadingById(id), toggles isFavorite boolean, updates via readingDao.update()
- observeFavorites(): Flow<List<Reading>> — calls readingDao.observeFavorites(), maps entities via toDomain(), distinctUntilChanged()
- observeFavoritesWithSpread(): Flow<List<ReadingWithSpread>> — calls readingDao.observeFavoritesWithSpread(), distinctUntilChanged()
Per D-02: Favorites format matches observeAllReadingsWithSpread() structure.
  </action>
  <verify>
  <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>ReadingRepository has toggleFavorite, observeFavorites, observeFavoritesWithSpread methods</done>
</task>

<task type="auto">
  <name>Task 3: Add tag-based reading query to ReadingTagDao</name>
  <files>app/src/main/kotlin/com/example/drawn/data/database/dao/ReadingTagDao.kt</files>
  <action>
Add method to ReadingTagDao (line ~25):
- @Query("SELECT DISTINCT readingId FROM reading_tags WHERE tagId IN (:tagIds)")
  fun observeReadingIdsByTags(tagIds: List<Long>): Flow<List<Long>>
Per D-01: Tag filter logic uses ANY (OR) — returns readings with ANY selected tag. Using IN clause achieves OR behavior.
  </action>
  <verify>
  <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>ReadingTagDao has observeReadingIdsByTags method</done>
</task>

<task type="auto">
  <name>Task 4: Add tag filter to ReadingRepository</name>
  <files>app/src/main/kotlin/com/example/drawn/data/repository/ReadingRepository.kt</files>
  <action>
Add method to ReadingRepository (requires ReadingTagDao dependency in constructor):
- Add readingTagDao: ReadingTagDao to constructor parameters
- Add observeReadingsByTags(tagIds: List<Long>): Flow<List<Reading>> — uses ReadingTagDao.observeReadingIdsByTags(tagIds), then fetches readings via readingDao.getReadingById() for each ID, maps to domain, applies distinctUntilChanged()
Requires adding ReadingTagDao to ReadingRepository constructor and DI in RepositoryModule.

Per D-01: Tag filter logic uses ANY (OR).
  </action>
  <verify>
  <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>ReadingRepository has observeReadingsByTags method</done>
</task>

<task type="auto">
  <name>Task 5: Update RepositoryModule DI</name>
  <files>app/src/main/kotlin/com/example/drawn/di/RepositoryModule.kt</files>
  <action>
Update RepositoryModule to provide all new repositories:
- Add imports for CustomCardDao, TagDao, ReadingTagDao
- Add imports for CustomCardRepository, TagRepository, ReadingTagRepository
- Add @Provides method for CustomCardRepository(customCardDao: CustomCardDao)
- Add @Provides method for TagRepository(tagDao: TagDao)
- Add @Provides method for ReadingTagRepository(tagDao: TagDao, readingTagDao: ReadingTagDao)
- Update provideReadingRepository to include readingTagDao: ReadingTagDao parameter
  </action>
  <verify>
  <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>RepositoryModule provides CustomCardRepository, TagRepository, ReadingTagRepository</done>
</task>

</tasks>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<verification>
All extensions compile without errors. ReadingRepository has favorites and tag filtering.
</verification>

<success_criteria>
Task 1: ReadingDao.observeFavorites() - ✅
Task 2: ReadingRepository.toggleFavorite(), observeFavorites() - ✅
Task 3: ReadingTagDao.observeReadingIdsByTags() - ✅
Task 4: ReadingRepository.observeReadingsByTags() - ✅
Task 5: RepositoryModule updated with all repositories - ✅
</success_criteria>

<output>
After completion, create `.planning/phases/09-repository-layer/09-02-SUMMARY.md`
</output>