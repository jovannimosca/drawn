---
phase: 09-repository-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/kotlin/com/example/drawn/data/repository/CustomCardRepository.kt
  - app/src/main/kotlin/com/example/drawn/data/repository/TagRepository.kt
  - app/src/main/kotlin/com/example/drawn/data/repository/ReadingTagRepository.kt
  - app/src/main/kotlin/com/example/drawn/data/repository/DeckRepository.kt
autonomous: true
requirements:
  - DECK-01
  - DECK-02
  - DECK-03
  - TAG-01
  - TAG-02
  - TAG-03
  - TAG-04
  - TAG-05
---

<objective>
Create repository layer with Flow-based data access for custom cards, tags, reading-tag associations, and deck favorites.
</objective>

<context>
@app/src/main/kotlin/com/example/drawn/data/database/dao/CustomCardDao.kt
@app/src/main/kotlin/com/example/drawn/data/database/dao/TagDao.kt
@app/src/main/kotlin/com/example/drawn/data/database/dao/ReadingTagDao.kt
@app/src/main/kotlin/com/example/drawn/data/database/entity/CustomCardEntity.kt
@app/src/main/kotlin/com/example/drawn/data/database/entity/TagEntity.kt
@app/src/main/kotlin/com/example/drawn/data/database/entity/ReadingTagCrossRef.kt
@app/src/main/kotlin/com/example/drawn/data/repository/CardRepository.kt
@app/src/main/kotlin/com/example/drawn/data/repository/DeckRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CustomCardRepository</name>
  <files>app/src/main/kotlin/com/example/drawn/data/repository/CustomCardRepository.kt</files>
  <action>
Create CustomCardRepository class following CardRepository pattern:
- Accept CustomCardDao via constructor dependency
- observeCardsForDeck(deckId: Long): Flow<List<CustomCard>> — uses CustomCardDao.observeCardsForDeck(), maps entities via CustomCardEntity.toDomain(), applies distinctUntilChanged()
- insert(card: CustomCard): Long — suspend function, calls CustomCardDao.insert(card.toEntity())
- update(card: CustomCard): Unit — suspend function, calls CustomCardDao.update(card.toEntity())
- delete(card: CustomCard): Unit — suspend function, calls CustomCardDao.delete(card.toEntity())
Use domain mapping functions from CustomCardEntity: toDomain() and toEntity().
  </action>
  <verify>
  <automated>kspIvy app/src/main/kotlin/com/example/drawn/data/repository/CustomCardRepository.kt compiles</automated>
  </verify>
  <done>CustomCardRepository with observeCardsForDeck, insert, update, delete methods compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create TagRepository</name>
  <files>app/src/main/kotlin/com/example/drawn/data/repository/TagRepository.kt</files>
  <action>
Create TagRepository class following CardRepository pattern:
- Accept TagDao via constructor dependency
- observeAllTags(): Flow<List<Tag>> — uses TagDao.observeAllTags(), maps entities via TagEntity.toDomain(), applies distinctUntilChanged()
- create(tag: Tag): Long — suspend function, calls TagDao.insert(tag.toEntity())
- update(tag: Tag): Unit — suspend function, calls TagDao.update(tag.toEntity())
- delete(tagId: Long): Unit — suspend function, calls TagDao.deleteById(tagId)
Use domain mapping functions from TagEntity: toDomain() and toEntity().
  </action>
  <verify>
  <automated>kspIvy app/src/main/kotlin/com/example/drawn/data/repository/TagRepository.kt compiles</automated>
  </verify>
  <done>TagRepository with observeAllTags, create, update, delete methods compiles</done>
</task>

<task type="auto">
  <name>Task 3: Create ReadingTagRepository</name>
  <files>app/src/main/kotlin/com/example/drawn/data/repository/ReadingTagRepository.kt</files>
  <action>
Create ReadingTagRepository class:
- Accept TagDao and ReadingTagDao via constructor dependency
- observeTagsForReading(readingId: Long): Flow<List<Tag>> — uses ReadingTagDao.observeTagIdsForReading(readingId), then fetches each tag via TagDao.getTagById(), maps to domain via TagEntity.toDomain(), applies distinctUntilChanged()
- addTag(readingId: Long, tagId: Long): Unit — suspend function, calls ReadingTagDao.insert(ReadingTagCrossRef(readingId, tagId))
- removeTag(readingId: Long, tagId: Long): Unit — suspend function, calls ReadingTagDao.delete(ReadingTagCrossRef(readingId, tagId))
- replaceTags(readingId: Long, tagIds: List<Long>): Unit — suspend function, deletes all via ReadingTagDao.deleteAllForReading(readingId), then inserts each tagId as new cross-ref
Per D-01 (tag filter logic): ANY (OR) — returns readings with any selected tag.
Use domain mapping functions from TagEntity: toDomain().
  </action>
  <verify>
  <automated>kspIvy app/src/main/kotlin/com/example/drawn/data/repository/ReadingTagRepository.kt compiles</automated>
  </verify>
  <done>ReadingTagRepository with observeTagsForReading, addTag, removeTag, replaceTags methods compiles</done>
</task>

<task type="auto">
  <name>Task 4: Extend DeckRepository with observeCustomDecks</name>
  <files>app/src/main/kotlin/com/example/drawn/data/repository/DeckRepository.kt</files>
  <action>
Add method to DeckRepository (line ~23 after observeBuiltInDecks):
- observeCustomDecks(): Flow<List<Deck>> — uses DeckDao.observeCustomDecks() (NEW QUERY: SELECT * FROM decks WHERE isCustom = 1), maps entities via DeckEntity.toDomain(), applies distinctUntilChanged()
Add to DeckDao: @Query("SELECT * FROM decks WHERE isCustom = 1") fun observeCustomDecks(): Flow<List<DeckEntity>>
  </action>
  <verify>
  <automated>kspIvy app/src/main/kotlin/com/example/drawn/data/repository/DeckRepository.kt compiles</automated>
  </verify>
  <done>DeckRepository.observeCustomDecks() method compiles and returns custom decks</done>
</task>

</tasks>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<verification>
All 4 repository files compile without errors. CustomCardRepository, TagRepository, ReadingTagRepository classes exist with required methods. DeckRepository has observeCustomDecks method.
</verification>

<success_criteria>
Task 1: CustomCardRepository with observeCardsForDeck Flow, insert/update/delete - ✅
Task 2: TagRepository with observeAllTags, create/update/delete - ✅  
Task 3: ReadingTagRepository with observeTagsForReading, addTag/removeTag/replaceTags - ✅
Task 4: DeckRepository.observeCustomDecks() - ✅
</success_criteria>

<output>
After completion, create `.planning/phases/09-repository-layer/09-01-SUMMARY.md`
</output>