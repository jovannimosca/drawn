---
phase: 01-foundation-dev-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle.kts
  - build.gradle.kts
  - app/build.gradle.kts
  - gradle/libs.versions.toml
  - gradle.properties
  - gradlew
  - gradlew.bat
  - gradle/wrapper/gradle-wrapper.jar
  - gradle/wrapper/gradle-wrapper.properties
  - app/src/main/AndroidManifest.xml
  - app/src/main/kotlin/com/example/drawn/DrawnApplication.kt
autonomous: true
requirements: [DEV-01, DEV-02]
must_haves:
  truths:
    - "Developer can run ./gradlew :app:assembleDebug and get a valid APK"
    - "All dependencies from version catalog resolve without conflicts"
    - "Project structure follows layer-based organization (ui/, data/, domain/)"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "All dependency versions and library aliases"
      contains: "kotlin, agp, compose-bom, hilt, room, coil"
    - path: "app/build.gradle.kts"
      provides: "App module configuration with all plugins and dependencies"
      contains: "plugins, android, dependencies blocks"
    - path: "app/src/main/AndroidManifest.xml"
      provides: "Android app manifest with MainActivity launcher"
      contains: "application, activity, intent-filter"
    - path: "app/src/main/kotlin/com/example/drawn/DrawnApplication.kt"
      provides: "Hilt Application entry point"
      contains: "@HiltAndroidApp"
  key_links:
    - from: "app/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "version catalog aliases (libs.xxx)"
      pattern: "libs\\."
    - from: "DrawnApplication.kt"
      to: "AndroidManifest.xml"
      via: "android:name attribute"
      pattern: "android:name=\"\\.DrawnApplication\""
---

<objective>
Create the complete Android project scaffolding with Gradle build configuration, version catalog, and all production dependencies. Establish the Application class and Android manifest.

Purpose: Provides the build foundation that all subsequent plans depend on. Without this, nothing else compiles.
Output: Buildable Android project with all dependencies declared per STACK.md and CONTEXT.md decisions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-foundation-dev-environment/01-CONTEXT.md

Key decisions from CONTEXT.md:
- D-09: Full production scaffolding from day one — not minimum viable
- D-10: Include Kotlin 2.2.21, AGP 9.1.0, Compose BOM 2025.12.00, Hilt 2.59.2, KSP, Navigation Compose 3 (1.0.1), Room 2.8.4, Material 3 (1.4.0), Coil 3.4.0
- D-11: CI config, Ktlint, Detekt set up in this phase
- D-12: JUnit 5 + MockK + Turbine test dependencies included
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gradle project structure with version catalog</name>
  <files>settings.gradle.kts, build.gradle.kts, gradle/libs.versions.toml, gradle.properties, gradle/wrapper/gradle-wrapper.properties</files>
  <action>
    Create the root-level Gradle configuration for the Drawn Android project:

    1. **settings.gradle.kts**: Configure plugin management with Google Maven, Maven Central, Gradle Plugin Portal. Set `rootProject.name = "drawn"`. Include `:app` module. Enable `enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")`.

    2. **build.gradle.kts** (root): Apply `android.application` and `kotlin.android` plugins via version catalog aliases. No buildscript block — use plugins DSL.

    3. **gradle/libs.versions.toml**: Define all versions and libraries per D-10 and STACK.md:
       - Versions: kotlin = "2.2.21", agp = "9.1.0", compose-bom = "2025.12.00", hilt = "2.59.2", ksp = "2.2.21-2.0.0", navigation-compose3 = "1.0.1", room = "2.8.4", material3 = "1.4.0", coil = "3.4.0", kotlinx-serialization = "1.8.0", kotlinx-coroutines = "1.10.1", lifecycle = "2.8.7", hilt-navigation-compose = "1.2.0", junit5 = "5.11.4", mockk = "1.13.16", turbine = "1.2.0", robolectric = "4.14.1", ktlint = "1.5.0", detekt = "1.23.7"
       - Libraries: all Compose BOM artifacts, Hilt + Hilt compiler, Room + Room compiler (KSP), Coil + Coil compose, Navigation Compose 3 + kotlinx-serialization, Kotlinx coroutines + Flow, Lifecycle runtime compose, Material 3, Hilt Navigation Compose
       - Test libraries: JUnit 5, MockK, Turbine, Compose UI test, Robolectric, Room testing (in-memory)
       - Plugins: android-application, kotlin-android, ksp, hilt

    4. **gradle.properties**: Set `org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8`, `android.useAndroidX=true`, `android.nonTransitiveRClass=true`, `kotlin.code.style=official`, `android.suppressUnsupportedCompileSdk=36`.

    5. **gradle/wrapper/gradle-wrapper.properties**: Use Gradle 8.13+ (compatible with AGP 9.1.0 and Kotlin 2.2.21). Set `distributionUrl` to gradle-8.13-bin.zip.

    Per D-09, this is full production scaffolding — include all dependencies from the start, even test libraries (D-12).
  </action>
  <verify>
    <automated>./gradlew tasks --all 2>&1 | head -20</automated>
  </verify>
  <done>Gradle sync succeeds, all version catalog entries resolve, project structure is valid Android project with :app module</done>
</task>

<task type="auto">
  <name>Task 2: Configure app module build.gradle.kts with all dependencies</name>
  <files>app/build.gradle.kts</files>
  <action>
    Create `app/build.gradle.kts` with complete production configuration per D-10:

    1. **Plugins**: `android.application`, `kotlin.android`, `com.google.devtools.ksp`, `dagger.hilt.android.plugin`, `org.jetbrains.kotlin.plugin.serialization`

    2. **Android block**:
       - `compileSdk = 36`
       - `defaultConfig`: `applicationId = "com.example.drawn"`, `minSdk = 26`, `targetSdk = 36`, `versionCode = 1`, `versionName = "1.0"`, `testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"`
       - `buildFeatures`: `compose = true`
       - `composeOptions`: `kotlinCompilerExtensionVersion` NOT needed — AGP 9.1.0 has built-in Compose compiler for Kotlin 2.0+
       - `buildTypes`: `release` with `isMinifyEnabled = false` (will enable later), `proguardFiles`
       - `compileOptions`: `sourceCompatibility = JavaVersion.VERSION_17`, `targetCompatibility = JavaVersion.VERSION_17`
       - `kotlinOptions`: `jvmTarget = "17"`
       - `namespace = "com.example.drawn"`
       - `testOptions`: Configure JUnit 5 with `unitTests.isReturnDefaultValues = true`

    3. **Dependencies** (all via version catalog):
       - Compose BOM platform import
       - Compose UI, Compose Foundation, Compose Material 3, Compose Animation, Compose UI Tooling Preview
       - Hilt Android + KSP for Hilt compiler
       - Room runtime + Room KTX + KSP for Room compiler
       - Coil + Coil compose + Coil network (for future use)
       - Navigation Compose 3 + kotlinx-serialization
       - Kotlinx coroutines Android + Kotlinx serialization JSON
       - Lifecycle runtime compose + Lifecycle ViewModel Compose
       - Hilt Navigation Compose
       - Debug: Compose UI Tooling, Compose UI Test Manifest
       - Test: JUnit 5, MockK, Turbine, Compose UI Test JUnit4, Robolectric, Room testing

    4. **KSP block**: Configure `arg("room.generateKotlin", "true")` for Kotlin-first Room code generation.

    5. **Compose compiler**: Use AGP 9.1.0's built-in Compose compiler (no separate plugin needed for Kotlin 2.0+).
  </action>
  <verify>
    <automated>./gradlew :app:dependencies --configuration releaseRuntimeClasspath 2>&1 | head -30</automated>
  </verify>
  <done>All dependencies declared and resolvable, app module compiles with Compose + Hilt + Room + Navigation Compose 3</done>
</task>

<task type="auto">
  <name>Task 3: Create AndroidManifest.xml and DrawnApplication class</name>
  <files>app/src/main/AndroidManifest.xml, app/src/main/kotlin/com/example/drawn/DrawnApplication.kt</files>
  <action>
    1. **AndroidManifest.xml**:
       - Package: `com.example.drawn`
       - `minSdkVersion="26"`, `targetSdkVersion="36"`
       - Application: `android:name=".DrawnApplication"`, `android:allowBackup="true"` (per PITFALLS.md Pitfall 16 — enables auto-backup for local data preservation)
       - `android:icon` and `android:label` placeholders (use `@mipmap/ic_launcher` and `@string/app_name`)
       - `android:theme` set to theme that will be defined in Plan 05
       - MainActivity with `LAUNCHER` intent filter, `exported="true"`
       - No camera/storage permissions needed yet — Photo Picker (Phase 3) doesn't require them

    2. **DrawnApplication.kt**:
       - Package: `com.example.drawn`
       - Extend `Application()` with `@HiltAndroidApp` annotation
       - Empty class body — Hilt generates the base class
       - This is the Hilt entry point required by all DI modules

    3. **String resources**: Create `app/src/main/res/values/strings.xml` with `<string name="app_name">Drawn</string>`

    4. **Directory structure**: Create the layer-based package structure per D-01:
       - `com.example.drawn.ui`
       - `com.example.drawn.data`
       - `com.example.drawn.domain`
       Each as empty packages (with `.gitkeep` or a placeholder file) to establish the architecture.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>APK builds successfully, DrawnApplication annotated with @HiltAndroidApp, manifest declares MainActivity as launcher, layer-based package structure exists</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug` completes without errors
- All dependencies from version catalog resolve
- DrawnApplication.kt exists with @HiltAndroidApp
- AndroidManifest.xml has correct SDK versions and MainActivity launcher
- Package structure: ui/, data/, domain/ directories exist under com.example.drawn
</verification>

<success_criteria>
- Gradle project syncs without errors
- APK builds successfully in debug mode
- All production dependencies declared (Hilt, Room, Compose, Navigation Compose 3, Coil)
- All test dependencies declared (JUnit 5, MockK, Turbine)
- Application class ready for Hilt DI
- Layer-based package structure established per D-01
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dev-environment/01-01-SUMMARY.md`
</output>
