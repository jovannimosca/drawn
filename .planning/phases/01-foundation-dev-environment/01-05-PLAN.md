---
phase: 01-foundation-dev-environment
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt
  - app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt
  - app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt
  - app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt
  - app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnDestinations.kt
  - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
  - app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt
  - app/src/main/kotlin/com/example/drawn/MainActivity.kt
  - app/src/main/res/values/colors.xml
  - app/src/main/res/values/themes.xml
autonomous: true
requirements: [DEV-02]
must_haves:
  truths:
    - "App launches directly to ReadingListScreen (no onboarding)"
    - "Empty state shows inviting message with visual cue for add action"
    - "Dark theme applied across all composables"
    - "FAB visible for future reading creation"
  artifacts:
    - path: "app/src/main/kotlin/com/example/drawn/MainActivity.kt"
      provides: "App entry point with @AndroidEntryPoint"
      contains: "@AndroidEntryPoint, setContent, DrawnTheme"
    - path: "app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt"
      provides: "Dark Material 3 theme wrapper"
      contains: "DrawnTheme, MaterialTheme"
    - path: "app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt"
      provides: "Navigation graph with ReadingList as start destination"
      contains: "NavHost, startDestination = ReadingList"
    - path: "app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt"
      provides: "Reading list screen with empty state and FAB"
      contains: "Scaffold, LazyColumn, FloatingActionButton"
    - path: "app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt"
      provides: "ViewModel with StateFlow UI state"
      contains: "@HiltViewModel, StateFlow, collectAsStateWithLifecycle"
  key_links:
    - from: "MainActivity.kt"
      to: "ui/theme/Theme.kt"
      via: "DrawnTheme composable wrapper"
      pattern: "DrawnTheme\\s*\\{"
    - from: "MainActivity.kt"
      to: "ui/navigation/DrawnNavHost.kt"
      via: "NavHost composition"
      pattern: "DrawnNavHost\\("
    - from: "ui/readinglist/ReadingListScreen.kt"
      to: "ui/readinglist/ReadingListViewModel.kt"
      via: "hiltViewModel() injection and collectAsStateWithLifecycle()"
      pattern: "hiltViewModel\\(\\)|collectAsStateWithLifecycle"
---

<objective>
Create the UI layer with Material 3 theme, Navigation Compose 3 routing, and the Reading List screen with empty state. Wire everything together in MainActivity.

Purpose: Delivers the first visible screen the user sees — an empty reading list with an inviting message. Establishes the navigation and theming foundation for all future screens.
Output: Dark theme, navigation graph, ReadingListScreen with empty state, MainActivity as entry point.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-dev-environment/01-CONTEXT.md
@.planning/phases/01-foundation-dev-environment/01-04-PLAN.md

Key decisions from CONTEXT.md:
- D-06: The app launches directly to the reading list screen
- D-07: Empty state shows a friendly message — "No readings yet, tap + to start" — with a visual cue for the add action
- D-08: No separate welcome/onboarding screen in v1
- D-10: Material 3 (1.4.0) for design system

Architecture patterns from ARCHITECTURE.md:
- Pattern 4: Sealed UI State Classes
- ViewModel exposes StateFlow, Compose collects via collectAsStateWithLifecycle()
- Navigation Compose 3 with type-safe @Serializable routes

Anti-patterns to avoid:
- Pitfall 2: LaunchedEffect self-cancellation — use snapshotFlow for state observation
- Pitfall 4: Events treated as state — use Channel for one-time events

Interfaces from Plan 04 (DI/Repository layer):
- ReadingRepository: observeAllReadings(): Flow<List<Reading>>
- Hilt: @HiltViewModel annotation, hiltViewModel() composable function
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Material 3 dark theme and Navigation Compose 3 setup</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/theme/Color.kt
    app/src/main/kotlin/com/example/drawn/ui/theme/Theme.kt
    app/src/main/kotlin/com/example/drawn/ui/theme/Type.kt
    app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnNavHost.kt
    app/src/main/kotlin/com/example/drawn/ui/navigation/DrawnDestinations.kt
  </files>
  <action>
    1. **Theme/Color.kt**: Define the dark mystical color palette (v1 baseline — Phase 3 will enhance with purples/golds):
       - `DarkBackground`, `DarkSurface`, `DarkPrimary`, `DarkOnPrimary`, `DarkSecondary`, `DarkOnSecondary`
       - Use Material 3 `ColorScheme` — start with dark defaults, Phase 3 will customize
       - `val DarkColorScheme = darkColorScheme(primary = ..., secondary = ..., tertiary = ..., background = ..., surface = ...)`

    2. **Theme/Theme.kt**:
       - `@Composable fun DrawnTheme(content: @Composable () -> Unit)` — wraps `MaterialTheme(colorScheme = DarkColorScheme, typography = DrawnTypography, content = content)`
       - Use `android:theme` in manifest to prevent white flash on launch (set to match DarkBackground)

    3. **Theme/Type.kt**:
       - `val DrawnTypography = Typography()` — use Material 3 default typography for now
       - Phase 3 will customize with mystical fonts

    4. **Navigation/DrawnDestinations.kt** — Type-safe route definitions per Navigation Compose 3:
       ```kotlin
       @Serializable
       object ReadingList

       @Serializable
       data class ReadingDetail(val readingId: Long)
       ```
       Use `@Serializable` from kotlinx-serialization (already in dependencies per Plan 01).

    5. **Navigation/DrawnNavHost.kt**:
       - `@Composable fun DrawnNavHost(navController: NavHostController, modifier: Modifier = Modifier)`
       - `NavHost(navController = navController, startDestination = ReadingList, modifier = modifier)`
       - `composable<ReadingList> { ReadingListScreen(...) }`
       - `composable<ReadingDetail> { backStackEntry -> ... }` — placeholder for Phase 2
       - Accept `onNavigateToReadingDetail: (Long) -> Unit` callback parameter for now

    Note: Theme is dark-only for v1 (per PROJECT.md constraint — local-only, personal use). No light theme toggle needed.
  </action>
  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>Theme compiles with dark color scheme. Navigation destinations defined with @Serializable. NavHost declares ReadingList as start destination and ReadingDetail as placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Create ReadingListScreen with empty state and wire MainActivity</name>
  <files>
    app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListScreen.kt
    app/src/main/kotlin/com/example/drawn/ui/readinglist/ReadingListViewModel.kt
    app/src/main/kotlin/com/example/drawn/MainActivity.kt
    app/src/main/res/values/colors.xml
    app/src/main/res/values/themes.xml
  </files>
  <action>
    1. **ReadingListViewModel.kt** — Hilt ViewModel with StateFlow:
       ```kotlin
       @HiltViewModel
       class ReadingListViewModel @Inject constructor(
           private val readingRepository: ReadingRepository
       ) : ViewModel() {
           val uiState: StateFlow<ReadingListUiState> = readingRepository.observeAllReadings()
               .map { readings -> ReadingListUiState.Success(readings) }
               .catch { error -> emit(ReadingListUiState.Error(error.message ?: "Unknown error")) }
               .stateIn(
                   scope = viewModelScope,
                   started = SharingStarted.WhileSubscribed(5_000),
                   initialValue = ReadingListUiState.Loading
               )
       }

       sealed interface ReadingListUiState {
           data object Loading : ReadingListUiState
           data class Success(val readings: List<Reading>) : ReadingListUiState
           data class Error(val message: String) : ReadingListUiState
       }
       ```

    2. **ReadingListScreen.kt** — Compose screen with empty state per D-07:
       - Accept `viewModel: ReadingListViewModel = hiltViewModel()` and `onAddReading: () -> Unit`
       - Collect state: `val uiState by viewModel.uiState.collectAsStateWithLifecycle()`
       - When `Success` with empty list: Show inviting empty state with icon, "No readings yet" text, and a visual cue (FAB or text hint) pointing to the + action
       - When `Success` with readings: Show LazyColumn with reading items (placeholder cards for now — Phase 2 will populate)
       - When `Loading`: Show CircularProgressIndicator
       - When `Error`: Show error message with retry option
       - Include a FloatingActionButton (FAB) with "+" icon that calls `onAddReading`
       - Use `Scaffold` with `topBar` showing "Drawn" app title

    3. **MainActivity.kt** — App entry point:
       - `@AndroidEntryPoint class MainActivity : ComponentActivity()`
       - `setContent { DrawnTheme { val navController = rememberNavController(); DrawnNavHost(navController = navController) } }`
       - Enable edge-to-edge display
       - Set `window.statusBarColor` to transparent for immersive feel

    4. **colors.xml and themes.xml** — Android XML resources:
       - `colors.xml`: Define `colorPrimary`, `colorPrimaryDark`, `colorAccent` matching the Compose dark theme
       - `themes.xml`: Create `Theme.Drawn` extending `Theme.Material3.Dark.NoActionBar` — this prevents the white flash before Compose renders
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>APK builds successfully. MainActivity launches to ReadingListScreen. Empty state shows inviting message with FAB. ViewModel collects Flow from repository. Navigation graph routes to ReadingList as start destination.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:assembleDebug` completes without errors
- Dark theme applied via DrawnTheme composable
- Navigation Compose 3 routes defined with @Serializable types
- ReadingListScreen shows empty state with "No readings yet, tap + to start" message
- FAB visible and calls onAddReading callback
- ViewModel uses StateFlow with collectAsStateWithLifecycle()
- MainActivity annotated with @AndroidEntryPoint
- XML theme prevents white flash on launch
</verification>

<success_criteria>
- App builds and can be installed on a physical Android device (DEV-02)
- App launches directly to ReadingListScreen (D-06)
- Empty state shows inviting message with visual cue for add action (D-07)
- No onboarding/welcome screen (D-08)
- Dark theme applied across all composables
- Navigation graph established with type-safe routes
- Hilt dependency injection working end-to-end (Application → Modules → ViewModel → Screen)
- FAB present for future reading creation (Phase 2)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dev-environment/01-05-SUMMARY.md`
</output>
